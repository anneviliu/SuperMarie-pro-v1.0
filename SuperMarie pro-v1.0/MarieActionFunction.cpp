#include"MarieActionfunction.h"
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <windows.h>

void move()
{
	char ch = _getch();
	if (ch == 0xe000)
	{
		ch = _getch();
		if (ch == 0x004b)
		{

			
		}
	}

	                         // 读取方向键
	                         
							 
							 
							 
							 // 匹配移动
}